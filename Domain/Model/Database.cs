using Microsoft.Extensions.Configuration;
using System.IO;
namespace Domain.Model
{
    public class Database
    {

        /* public IConfigurationRoot GetConfiguration()
         {
             var builder = new ConfigurationBuilder().(Directory.GetCurrentDirectory()).Add
             }   
        */
    }
}
